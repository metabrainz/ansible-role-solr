#jinja2: trim_blocks: True, lstrip_blocks: True
# {{ ansible_managed }}
{% import "property-macros.j2" as macros without context %}

{% for property, value in (item.value | dictsort) %}
{{ macros.format_property(property, value) -}}
{% endfor %}
